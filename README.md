
# ai-context — Инструмент для управления контекстом AI-приложений

> Этот pet-проект реализует идеи О. Чекирова: локальный CLI-инструмент для автоматического сбора контекста 
> кодовой базы и взаимодействия с ИИ **в контексте проекта**. Дальнейшее развитие идеи привнесло в проект 
> встроенный чат с нейросетью, отслеживание и компрессию файлов, 
> несколько вариантов контекста ("контекст", как полный текст файла и "резюме", 
> как верхнеуровневое описание структур, классов и функций проекта)
> 
> Проект и далее будет разрабатываться, новые функции будут добавляться постепенно.

## 📌 Описание

`ai-context` — это языкoнезависимый CLI-инструмент для разработчиков, который:

- Автоматически анализирует структуру и содержимое проекта  
- Поддерживает актуальный контекст в реальном времени  
- Позволяет вести диалог с локальной ИИ-моделью (например, через Ollama), передавая ей **полный контекст проекта**  
- Предназначен для личного использования, с возможностью публикации как open-source  

Поддерживает **любые языки программирования** и **любые структуры проектов**.

---

## 📦 Основные функции

### 1. **Индексация файлов**
- Рекурсивно сканирует проект
- Учитывает правила из `.ai-context/.ai-ignore` (совместим с `.gitignore`)
- Пропускает:
  - бинарные файлы  
  - файлы > 1 МБ  
  - скрытые файлы/папки (если не разрешены)
- Сохраняет содержимое в SQLite-базу (`context.db`)  
- Автоматически добавляет `.ai-context/` в `.gitignore`

### 2. **Сжатие данных**
- Генерирует **резюме проекта**: только сигнатуры функций/классов и первые строки докстрингов
- Поддерживает извлечение сигнатур только для Python (через AST)
- Резюме **кэшируется в БД** — не перегенерируется при `compress`
- Экспорт: `resume.txt`

### 3. **Интерактивный чат**
- Поддерживает историю диалога с ограничением по токенам
- Автоматически загружает:
  - системный промпт (`system-prompt.txt`)
  - резюме проекта
  - полный контекст (опционально)
- Использует многошаговую инициализацию (`step_1`, `step_2`, `step_3`)
- Сохраняет историю в `dialog.json`
- Поддерживает streaming (в будущем) и локальные модели через OpenAI-совместимый API

### 4. **Автоматическое обновление**
- Запускает фоновый демон (`watchdog`)
- Отслеживает события: создание, изменение, удаление файлов
- Автоматически обновляет или удаляет файлы в `context.db`
- Пересчитывает кэш резюме при каждом изменении

### 5. **Экспорт контекста**
- Экспортирует содержимое БД в человекочитаемый текстовый файл (`out.txt`)
- Формат:
  ```text
  ### FILE: путь/к/файлу ###
  <содержимое>
  ============================================================
  ```

---

## 🛠️ Установка

1. Установите зависимости:
   ```bash
   pip install -r requirements.txt
   ```

2. Инициализируйте проект:
   ```bash
   ai-context init
   ```
   Эта команда:
   - создаёт `.ai-context/`  
   - генерирует `secrets.json`, `system-prompt.txt`, `.ai-ignore`  
   - запускает первичную индексацию  
   - экспортирует полный контекст в `./out.txt`

> ⚠️ **Важно**: файл `secrets.json` содержит API-ключи и **никогда не должен коммититься в Git**. Он автоматически исключён через `.gitignore`.

---

## 📌 Использование

### Команды

| Команда | Назначение |
|--------|-----------|
| `ai-context init` | Инициализация проекта |
| `ai-context index` | Однократная индексация файлов |
| `ai-context compress [--output resume.txt]` | Экспорт кэшированного резюме |
| `ai-context read ./output.txt` | Экспорт полного контекста из БД |
| `ai-context chat [--clear]` | Интерактивный чат с ИИ |
| `ai-context prompt` | Редактирование системного промпта |
| `ai-context watchdog [--stop]` | Запуск/остановка фонового наблюдателя |

### Пример

```bash
ai-context init
ai-context compress --output resume.txt
ai-context chat
```

---

## 🗂️ Структура проекта

```
.ai-context/
├── .ai-ignore         # Правила исключения файлов
├── secrets.json       # URL и API-ключи (НЕ КОММИТИТЬ!)
├── system-prompt.txt  # Системный промпт для ИИ
├── dialog.json        # История диалога
├── context.db         # Основное хранилище контекста (SQLite)
└── context.txt        # Резервный экспорт для отладки

ai-context/
├── commands/
│   ├── chat.py        # Обработка чата
│   ├── compress.py    # Сжатие данных
│   ├── index.py       # Индексация файлов
│   ├── init.py        # Инициализация
│   ├── prompt.py      # Редактирование промпта
│   └── watchdog.py    # Наблюдатель за файлами
├── source/
│   ├── messages.py    # Цветовые константы
│   └── settings.py    # Настройки
├── ai_context/
│   ├── cli.py         # CLI-интерфейс
│   └── __init__.py
├── ai-chat-test.py    # Тестовый скрипт для подсчёта токенов
└── README.md
```

---

## ⚙️ Технический стек (v1)

| Компонент | Технология |
|----------|-----------|
| Язык | Python 3.10+ |
| CLI | Typer + Rich |
| Игнор-правила | `pathspec` (совместимость с `.gitignore`) |
| Наблюдатель | `watchdog` |
| ИИ-клиент | OpenAI SDK (с поддержкой Ollama через `base_url`) |
| Хранение | SQLite (`context.db`) — основной формат |
| Токенизация | `tiktoken` (fallback: `cl100k_base`) |
| Локальная модель | `qwen3:14b` или `deepseek-coder:6.7b-instruct` через Ollama (`http://localhost:11434/v1`) |

---

## 📚 Лицензия

MIT — см. файл [`LICENSE`](LICENSE)
